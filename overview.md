# BlueNote Overview

The detailed project overview now lives at:

- `docs/product/overview.md`
- `docs/architecture/`
- `docs/phases/`

This root file is intentionally short so the repo entrypoint stays clear while the durable planning and architecture material lives under `docs/`.
